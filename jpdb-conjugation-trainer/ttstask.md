I have just handed you access to an 11Labs key. 11Labs is a platform that allows you to generate text-to-speech. Let's add a new feature to the application that provides text-to-speech for the conjugated words. Specifically, the application should auto-generate a voice line for the conjugated word that will be played on the correct or wrong page. We don't use the user's input as the reference, of course, but instead we use whatever the LLM has told us. Generating text-to-speech is generally an expensive operation, so we will need to do file system caching on the backend. There should be a new directory that contains all the words that have a TTS-generated voice line associated with them. Before hitting the 11Labs endpoint to generate a new TTS, the backend should look them up in that directory and serve a potentially existing TTS voice line.