### Task Completion Guidelines

Before finishing any task, you must ensure that the project is in a valid state.

- Always run `npm run validate` from the root directory before finishing a task. This command performs type-checking, linting, and runs unit tests for both the root project and the TTS microservice.
- Ensure that `npm run validate` passes completely with no errors.
